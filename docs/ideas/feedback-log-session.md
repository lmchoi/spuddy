
- when back button is pressed:
    - warn user that they are cancelling workout? or if they say don't cancel, we should store the data so they can resume?
    - when they press + again, they can either resume? or start fresh?

- button to finish workout so they don't need to do the last set of the last exercise, should always be available - perhaps in the top right corner?

- can add another set? (maybe no need to remove for now?)

- should show the BPs if available - suggest what are BPs is useful to show? 

why is the increment button weird? what logic is used?
- the plus button for overhead press (starts at 18) is weird, it went down to 17.5
- the plus button for triceps pushdown is werd (starts at 12), goes up to 20

- ability to save as new day or replace this one

- ability to swap exercise (might defer until we integrate with the exercise db)
- ability add new exercise

